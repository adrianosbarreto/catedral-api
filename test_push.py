import requests

def send_notification():
    # Isso simula o request para a rota do Backend (que precisa de auth).
    # Dado que isso seria local, seria mais fácil testar abrindo o App, 
    # efetuando o login e chamando o endpoint com o Token JWT, 
    # ou podemos só validar diretamente no painel de Notícias se conectarmos.
    pass

if __name__ == "__main__":
    send_notification()
